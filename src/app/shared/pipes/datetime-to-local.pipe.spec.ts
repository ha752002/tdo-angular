import { DatetimeToLocalPipe } from './datetime-to-local.pipe';

describe('DatetimeToLocalPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeToLocalPipe();
    expect(pipe).toBeTruthy();
  });
});
